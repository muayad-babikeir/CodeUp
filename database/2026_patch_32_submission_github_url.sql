-- ============================================================
-- CodeUp — Patch #32: رابط GitHub اختياري بتسليم الواجب
-- ============================================================
-- عمود نصي بسيط، اختياري تمامًا، جنب رفع الملف الحالي (لا يستبدله).
-- لا حاجة لجدول جديد ولا RLS إضافية — نفس صلاحيات submissions الحالية
-- تغطيه تلقائيًا لأنه عمود بنفس الجدول.
-- ============================================================

alter table submissions add column if not exists github_url text;

comment on column submissions.github_url is 'رابط ريبو GitHub اختياري يرفقه الطالب مع التسليم، بديل أو إضافة لرفع ملف';
